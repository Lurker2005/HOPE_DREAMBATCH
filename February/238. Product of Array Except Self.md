## Array
```java
class Solution {
    public int[] productExceptSelf(int[] nums) {
        int n = nums.length;
        int ans[] = new int[n];
        Arrays.fill(ans, 1);
        int curr = 1;
        for(int i = 0; i < n; i++) {
            ans[i] *= curr;
            curr *= nums[i];
        }
        curr = 1;
        for(int i = n - 1; i >= 0; i--) {
            ans[i] *= curr;
            curr *= nums[i];
        }
        return ans;
    }
}
```
![Submission screenshots](screenshots/Product%20of%20array%20except%20self/Array.png)
## prefix sum
```java
class Solution {
    public int[] productExceptSelf(int[] nums) {
        int totalProduct = 1;
        int zeroCount = 0;

        for (int num : nums) {
            if (num != 0) {
                totalProduct *= num;
            } else {
                zeroCount++;
            }
        }

        int[] result = new int[nums.length];


        if (zeroCount > 1) {
            return result; 
        }


        for (int i = 0; i < nums.length; i++) {
            if (nums[i] != 0) {
                if (zeroCount == 1) {
                    result[i] = 0;
                } else {
                    result[i] = totalProduct / nums[i];
                }
            } else {
                result[i] = totalProduct;
            }
        }

        return result;
    }
}
```
![Submission screenshots](screenshots/Product%20of%20array%20except%20self/prefixsum.png)