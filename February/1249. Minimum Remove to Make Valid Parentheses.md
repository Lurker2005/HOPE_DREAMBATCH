## String (StringBuilder)

```java
class Solution {
    public String minRemoveToMakeValid(String s) {

        StringBuilder sb = new StringBuilder();
        int open = 0;


        for(char c : s.toCharArray()){

            if(c == '('){
                open++;
            }
            else if(c == ')'){
                if(open == 0) continue;
                open--;
            }

            sb.append(c);
        }


        for(int i = sb.length()-1; i>=0 && open>0; i--){
            if(sb.charAt(i) == '('){
                sb.deleteCharAt(i);
                open--;
            }
        }

        return sb.toString();
    }
}
```
![Submission screenshots](screenshots/Minimum%20Remove%20to%20Make%20Valid%20Parentheses/String.png) 
## Stack

```java
class Solution {
    public String minRemoveToMakeValid(String s) {

        Stack<Integer> st = new Stack<>();

        for(int i = 0; i < s.length(); i++){
            char c = s.charAt(i);

            if(c == '('){
                st.push(i);
            }
            else if(c == ')'){
                if(!st.isEmpty() && s.charAt(st.peek()) == '('){
                    st.pop();  
                }
                else{
                    st.push(i); 
                }
            }
        }


        HashSet<Integer> remove = new HashSet<>();

        while(!st.isEmpty()){
            remove.add(st.pop());
        }


        StringBuilder res = new StringBuilder();

        for(int i = 0; i < s.length(); i++){
            if(!remove.contains(i)){
                res.append(s.charAt(i));
            }
        }

        return res.toString();
    }
}
```
![Submission screenshots](screenshots/Minimum%20Remove%20to%20Make%20Valid%20Parentheses/Stack.png) 